apiVersion: v1
kind: Service
metadata:
  name: docker-test
  labels:
    app: docker-test
spec:
  type: NodePort
  selector:
    app: docker-test
  ports:
    - port: 80
      targetPort: 80
      nodePort: 30080
